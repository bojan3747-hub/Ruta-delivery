# PostgreSQL connection string (Supabase or Neon in production, local Postgres in dev)
DATABASE_URL="postgresql://user:password@localhost:5432/ruta"
SESSION_SECRET="replace-with-a-long-random-string"
